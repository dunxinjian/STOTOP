# CardFlow 设计器重设计 · 实施进度（2026-07-08）

> 配合 `2026-07-07-cardflow-设计器重设计落地-总体plan.md`。分支 `worktree-cardflow-designer-v2`（worktree `.claude/worktrees/cardflow-designer-v2`，base=master a6d96e5）。**未 push**。

## 已完成（35+ commit，type-check/vitest/build-filter cardflow 全绿）

### M0 令牌与共享原语 ✅（5/5）
- M0-1 流程色令牌 flow-auto/flow-cc/shadow-lift（theme.ts+TOKENS.md+variables.scss）
- M0-2 边界态三组件 + cardflowConditionFormat 纯函数（formatCondition/summarizeConditions/summarizeMembers，16 用例）
- M0-3 PermissionTri 权限胶囊（四态 editable/readonly/masked/hidden，对齐 StageViewProfile access）
- M0-4 SaveStateChip 四态胶囊（接入编辑页顶栏）
- M0-5 UI 保真基准：cardflow-designer.scss（cfd- 前缀移植 mock-shared.css）+ ui-baseline.md 三表

### M1 竖向流程图 ✅（6/6，核心批）
- M1-1 buildFlowTree 投影纯函数（分支组/兜底/环 complex/交叉边 stageRef/legacy 线性/孤儿追加，23 用例）
- M1-2/M1-3 FlowVerticalGraph 渲染+四类插入菜单+编辑页 STEP_STAGES 接入（segmented 流程视图/节点链/只读总览图）
- M1-4 分支删除保护/复制/调序（deleteBranch/copyBranch/reorderBranch + BranchDeleteConfirm）
- M1-5 诊断定位脉冲 + FlowStateCanvas 降级只读
- M1-6 dev 库 44/45 冒烟（守恒判据）+ 两真实形态修复（#2261 部分汇合/#1357 迁移遗留孤儿）

### M2 节点抽屉五 Tab + 节点变体 ✅（前后端）
- M2-1/M2-5 五 Tab 重构（基础/处理人/字段权限/动作/高级）+ PermissionTri 接线+表头批量+明细列级
- M2-2 autoDecision 审批类型三态（引擎 TryApplyAutoDecisionAsync 消费，choke=AssignStageHandlersAsync）+ UI
- M2-3 处理人 Tab orgChain 策略/三选兜底/转指定人
- M2-6 意见必填（引擎 EnsureOpinionProvidedAsync 事务外校验 400）+ UI
- M2-7 StageTimeoutReminderJob 一级超时提醒（ForEachActiveTenantAsync，seeder V68）
- M2-8/M2-9 凭证试算按钮位预留 + 发起节点说明弹层
- **M2-4 会签比例通过**：引擎无消费 → 裁剪进 M8（不做假配置）

### M3 条件编辑器 ✅（4/4）
- M3-1/M3-2 条件组容器视觉（组内且/组间或）+ 算子按引擎全集完备
- M3-3 字段下拉分组+类型徽标+禁用说明
- M3-4 路由命中率试算端点 route-hit-estimate + 三态展示

### M4 矩阵+路由锁 ✅（4/4）
- M4-1 buildRouteFieldIndex 路由引用索引 + 被引用字段删除拦截
- M4-2/M4-3 FieldPermissionMatrix 矩阵视图+敏感漏配诊断+列头批量
- M4-4 摘要字段敏感禁选

### M5 干跑工作台 ⏳（2/5）
- M5-0 四步向导对齐 mock + 干跑工作台化 ✅
- M5-4 干跑命中路径点亮竖向图 ✅
- **M5-1 样例值三来源 / M5-2 失败态推演 / M5-3 凭证试算**：后端代理进行中（sample-cards/失败态/凭证试算端点）

### M6 发布与版本 ✅（后端 4 端点 live 实测 + 前端全套）
- #3 inflight-summary 在途计数 / #2 create-draft 回滚 / #4 publish migrate 迁移（服务接线+seeder V69 迁移日志表）
- M6-1 flowVersionDiff 纯函数（8 用例）/ M6-2 PublishConfirmModal / M6-4 VersionHistoryDrawer 回滚 / M6-6 列表页放弃草稿
- **live 实测**：worktree 后端起服，inflight-summary/versions/publish(body) 端点 200 全通

## 待完成
- **M5-1/M5-2/M5-3**（后端代理 a69450566b44dd068 进行中）
- **M7**：M7-1 编辑锁+接管（后端锁表+SignalR，#9）/ M7-3 插入动效 / M7-4 键盘导航+ARIA / M7-5 断点+骨架。M7-2 撤销历史下拉已在早期 commit 844982e 完成
- **整体终审 + 全量回归**
- **M8 二期立项**（导入双模/引擎增强四件/发起节点三件/模板库）

## 关键运行须知
- worktree 后端起服需先拷 `src/STOTOP.WebAPI/db-connections.json`（不在 git）；seeder 到 V69。
- preview MCP 工具本会话不可用，UI 验证靠 Vite transform 200 + DOM 断言 + type-check/vitest。
- 后端 dll 锁：主树与 worktree 后端不能同时跑（都占 9000），起 worktree 后端前先杀 9000。
- CardFlow.Tests 364+ 全绿（含 M2 新增 9 + M6 迁移）；前端 vitest 106+ 全绿。
