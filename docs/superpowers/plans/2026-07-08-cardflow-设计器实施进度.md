# CardFlow 设计器重设计 · 实施进度（2026-07-08，更新于 M7-1 编辑锁验证通过后）

> 配合 `2026-07-07-cardflow-设计器重设计落地-总体plan.md`。分支 `worktree-cardflow-designer-v2`（worktree `.claude/worktrees/cardflow-designer-v2`，base=master a6d96e5）。**未 push**。HEAD=`f0417ff`，M7-1 六个 commit 已核实（`b35a6f6`~`5507ad2`）。

## 已完成（40+ commit，type-check/vitest/build-filter cardflow 全绿）

### M0 令牌与共享原语 ✅（5/5）
- M0-1 流程色令牌 flow-auto/flow-cc/shadow-lift（theme.ts+TOKENS.md+variables.scss）
- M0-2 边界态三组件 + cardflowConditionFormat 纯函数（formatCondition/summarizeConditions/summarizeMembers，16 用例）
- M0-3 PermissionTri 权限胶囊（四态 editable/readonly/masked/hidden，对齐 StageViewProfile access）
- M0-4 SaveStateChip 四态胶囊（接入编辑页顶栏）
- M0-5 UI 保真基准：cardflow-designer.scss（cfd- 前缀移植 mock-shared.css）+ ui-baseline.md 三表

### M1 竖向流程图 ✅（6/6，核心批）
- M1-1 buildFlowTree 投影纯函数（分支组/兜底/环 complex/交叉边 stageRef/legacy 线性/孤儿追加，23 用例）
- M1-2/M1-3 FlowVerticalGraph 渲染+四类插入菜单+编辑页 STEP_STAGES 接入（segmented 流程视图/节点链/只读总览图）
- M1-4 分支删除保护/复制/调序（deleteBranch/copyBranch/reorderBranch + BranchDeleteConfirm）
- M1-5 诊断定位脉冲 + FlowStateCanvas 降级只读
- M1-6 dev 库 44/45 冒烟（守恒判据）+ 两真实形态修复（#2261 部分汇合/#1357 迁移遗留孤儿）

### M2 节点抽屉五 Tab + 节点变体 ✅（前后端）
- M2-1/M2-5 五 Tab 重构（基础/处理人/字段权限/动作/高级）+ PermissionTri 接线+表头批量+明细列级
- M2-2 autoDecision 审批类型三态（引擎 TryApplyAutoDecisionAsync 消费，choke=AssignStageHandlersAsync）+ UI
- M2-3 处理人 Tab orgChain 策略/三选兜底/转指定人
- M2-6 意见必填（引擎 EnsureOpinionProvidedAsync 事务外校验 400）+ UI
- M2-7 StageTimeoutReminderJob 一级超时提醒（ForEachActiveTenantAsync，seeder V68）
- M2-8/M2-9 凭证试算按钮位预留 + 发起节点说明弹层
- **M2-4 会签比例通过**：引擎无消费 → 裁剪进 M8（不做假配置）

### M3 条件编辑器 ✅（4/4）
- M3-1/M3-2 条件组容器视觉（组内且/组间或）+ 算子按引擎全集完备
- M3-3 字段下拉分组+类型徽标+禁用说明
- M3-4 路由命中率试算端点 route-hit-estimate + 三态展示

### M4 矩阵+路由锁 ✅（4/4）
- M4-1 buildRouteFieldIndex 路由引用索引 + 被引用字段删除拦截
- M4-2/M4-3 FieldPermissionMatrix 矩阵视图+敏感漏配诊断+列头批量
- M4-4 摘要字段敏感禁选

### M5 干跑工作台 ✅（5/5，功能完成；浏览器人工验证未做）
- M5-0 四步向导对齐 mock + 干跑工作台化 ✅
- M5-4 干跑命中路径点亮竖向图 ✅
- M5-1 样例值三来源 + 历史卡片采样端点（455ecbc）✅
- M5-2 干跑失败态推演——不终止按兜底继续并标注（087757f）✅
- M5-3 凭证试算端点（诚实降级）+ 自动节点分录预览（cf1a77e）✅
- **注意**：三者均只过 type-check + vitest，**未做真实浏览器 UI 交互验证**（无 preview 工具可用），新会话若要收尾建议补一次人工/浏览器验证

### M6 发布与版本 ✅（后端 4 端点 live 实测 + 前端全套）
- #3 inflight-summary 在途计数 / #2 create-draft 回滚 / #4 publish migrate 迁移（服务接线+seeder V69 迁移日志表）
- M6-1 flowVersionDiff 纯函数（8 用例）/ M6-2 PublishConfirmModal / M6-4 VersionHistoryDrawer 回滚 / M6-6 列表页放弃草稿
- **live 实测**：worktree 后端起服，inflight-summary/versions/publish(body) 端点 200 全通

### M7 收尾 ✅（4/5 完成，1 项暂缓）
- M7-1 编辑锁+接管协议（`b35a6f6`~`5507ad2`，6 commit）✅——`CfDefinitionEditLock` 实体(`IOrgScoped+ITenantScoped`)+seeder V70 建表；`DefinitionEditLockService` 心跳续期(30s)/死锁判定(120s)/超时移交(60s)/原子移交（单 SaveChanges 唯一写者不变量）；`FlowDefinitionLockController` 五端点(acquire/heartbeat/takeover-request/takeover-respond/release)；SignalR `CardFlowHub` flowdef-{id} 组订阅；前端 `useDefinitionEditLock` composable + 编辑页只读横幅/接管弹窗接线；并发获取抗竞态修复(5507ad2，避免唯一索引冲突 500)。**本轮补验**：`CardFlow.Tests` 全量重跑 378 passed/0 failed/4 skipped（含 `DefinitionEditLockTests.cs` 6 个新增用例，覆盖并发接管仅一胜/原子移交/死锁抢占），非道听途说的 commit 信息，是实测结果。
- M7-2 撤销历史下拉（早期 commit 844982e）✅
- M7-3 竖向图节点生长/分支展开入场动效（f9e0af9）✅
- M7-4 竖向图键盘导航 + ARIA 树语义（6dfe854）✅
- **M7-5 断点+骨架屏**：暂缓（现有 a-spin 已覆盖基本加载体验，优先级低）

### 整体终审 ✅（三修已 commit 140ac10）
general-purpose 子代理对抗性只读审查发现 3 个真实 bug，均已修复并验证（投影 28 用例全绿 + type-check 通过）：
- **bug#1 [HIGH]** `FlowDefinitionEditPage.vue` `loadPublishedSnapshot` 读 `s.name` 恒 undefined（DTO 真字段是 `stageName`），导致发布确认弹窗把每个保留节点误报为"名称修改"
- **bug#2 [HIGH]** `flowGraphProjection.ts` `insertBranchGroup` 在已分支节点上二次插入会清空全部兄弟分支致数据丢失+孤儿节点；已加回归测试用例
- **bug#3 [MED]** 权限 `readonly` 切换后残留 `required:true` 产生"只读且必填"矛盾配置；`FieldPermissionMatrix.vue` 与 `StageConfigPanel.vue` 两处 `setAccess`/`setFieldAccess` 均已同步修复

## 待完成
- **M7-5 断点+骨架**：低优先级，可暂缓
- **M5 浏览器人工验证**：建议在有 preview 工具的环境里跑一遍三个新交互（样例来源/失败态标注/凭证试算按钮）
- **全量最终回归**：`npm run type-check` + `npx vitest run` + `build-filter cardflow` + `CardFlow.Tests` 完整跑一遍——`CardFlow.Tests` 本轮已重跑（378 passed），其余三项仍待本轮内一次性收口
- **开 PR**：功能已足够完整，收尾回归后可用 `finishing-a-development-branch` 走流程（worktree 分支 → 目标 master，未 push）
- **M8 二期立项**（导入双模/引擎增强四件/发起节点三件/模板库）——仅立项不在本轮范围

## 关键运行须知
- worktree 后端起服需先拷 `src/STOTOP.WebAPI/db-connections.json`（不在 git）；seeder 到 V69。
- preview MCP 工具本会话不可用，UI 验证靠 Vite transform 200 + DOM 断言 + type-check/vitest。
- 后端 dll 锁：主树与 worktree 后端不能同时跑（都占 9000），起 worktree 后端前先杀 9000。
- CardFlow.Tests 378 passed/0 failed/4 skipped（含 M2 新增 9 + M6 迁移 + M7-1 编辑锁 6 用例，2026-07-08 本轮实测）；前端 vitest 106+ 全绿。
